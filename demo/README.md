Demo
====

To build and run the demo with `cmake`, run the following command in the parent directory of the folder containing this README:

```bash
mkdir -p cmake-build-demo
cd cmake-build-demo
cmake ..
make
./demo
```

Then you should be able to see an SVG file `demo.csv`:

![](demo.svg)
